var newSpawnFunction = {
    /**
     * 
     * @param {StructureSpawn} target 
     */
    run: function(target){
        var spawn = target;
        var workList = ['harvester','upgrader'];

        var sourceApart = _.filter(target.room)

    }
}
